import Types from '../constants/actiontypes'

const camera = (
	state = {
		position: new THREE.Vector3(0, 0, 5),
	}, action) => {

	switch(action.type) {
		default:
			return state
	}

}

export default camera
